TMDB_API_KEY=your_tmdb_api_key_here
ENV=development

# Secret partagé avec l'app (X-App-Token header) pour sécuriser le proxy IA.
# Si absent, la vérification est désactivée (mode dev local).
# En production Railway : mettre la même valeur que APP_TOKEN dans llm.ts.
APP_TOKEN=cinepick-v1
